# Sinh AgeOS.xcodeproj bằng: xcodegen generate (chạy trong apps/AgeOS/)
# Bundle id chốt theo GitHub owner olbboy (repo public 30/8/2026).
name: AgeOS
options:
  deploymentTarget:
    macOS: "26.0"
  createIntermediateGroups: true

packages:
  AgeOSPackage:
    path: ../..

targets:
  AgeOS:
    type: application
    platform: macOS
    sources:
      - Sources
    dependencies:
      - package: AgeOSPackage
        product: AgeOSCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.olbboy.ageos
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "6.0"
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGN_STYLE: Manual
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_NSPrincipalClass: NSApplication
        INFOPLIST_KEY_CFBundleDisplayName: AgeOS
        COMBINE_HIDPI_IMAGES: YES
        # Bắt buộc bật tay: project do xcodegen sinh KHÔNG thừa hưởng mặc định
        # của template Xcode. Thiếu hai dòng này thì Localizable.xcstrings sinh ra
        # rỗng, không lỗi, không cảnh báo. Đã đo bằng -showBuildSettings.
        SWIFT_EMIT_LOC_STRINGS: YES
        LOCALIZATION_PREFERS_STRING_CATALOGS: YES

  AgeOSTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests
    dependencies:
      - target: AgeOS
    settings:
      base:
        SWIFT_VERSION: "6.0"
        GENERATE_INFOPLIST_FILE: YES
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/AgeOS.app/Contents/MacOS/AgeOS"
        BUNDLE_LOADER: "$(TEST_HOST)"

  AgeOSUITests:
    type: bundle.ui-testing
    platform: macOS
    sources:
      - UITests
    dependencies:
      - target: AgeOS
    settings:
      base:
        SWIFT_VERSION: "6.0"
        GENERATE_INFOPLIST_FILE: YES
        TEST_TARGET_NAME: AgeOS

schemes:
  AgeOS:
    build:
      targets:
        AgeOS: all
    test:
      targets:
        - AgeOSTests
        - AgeOSUITests
